{
    "name": "fullstack-app",
    "version": "1.0.0",
    "scripts": {
      "build": "npm run build",
      "start": "npm start",
      "dev": "concurrently \"cd server && npm run dev\" \"cd test-mail && npm start\"",
      "install-client": "npm install",
      "install-server": "npm install",
      "install-all": "npm run install-server && npm run install-client"
    },
    "devDependencies": {
      "concurrently": "^7.6.0"
    }
  }
